calculates realistic price averages from noisy datasets by mimicking intuitive human estimation. It computes a baseline mean, progressively strips away the most extreme outliers (scams, mispriced listings, and anomalies), and recalculates the average until all remaining data converges within a specified variance threshold.
